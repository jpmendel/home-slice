[Unit]
Description=wsgi service
Requires=app.socket
After=network.target

[Service]
User=pi
Group=www-data
WorkingDirectory=/home/pi/app
ExecStart=/home/pi/app/.venv/bin/gunicorn \
    --workers 3 \
    --bind unix:/run/app.sock \
    src.main.wsgi:application

[Install]
WantedBy=multi-user.target